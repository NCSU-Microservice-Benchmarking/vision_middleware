export default async function buildRandomSeed(videoUUID: string, instanceID: string): string {
    // Placeholder implementation for building a random seed
    return `${videoUUID}-${instanceID}-seed`;
}